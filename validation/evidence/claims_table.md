| # | FINDINGS.md line | claim | query that would prove it |
|---|---|---|---|
| 5.1 | 9 | "All 18 scenes probed `planar_z` with confident margins." | `# Not a parquet claim: depth-convention classification lives in scene manifests. # Closest parquet-side check of the scene population: df = pd.read_parquet("outputs/eval/experiment_zero.parq` |
| 5.2 | 14 | "A fronto-parallel constancy test for the depth convention failed on 9 of 18 scenes because probe floors are slightly sloped planes. Classifying by ro | `# No parquet column carries probe-classification method or per-probe fitted spread. # Would require: data/replica_renders/*/probes/classification.json # counts = {s: json.load(open(f"data/re` |
| 5.3 | 21 | "their navigable points stand outside the rendered shell (straight-down floor views were 0 to 23 percent valid)" | `# No parquet column for navmesh floor-visibility validity fraction. # Would require the renderer's navmesh verification log per scene: # v = [probe["valid_fraction"] for probe in manifest["n` |
| 5.4 | 23 | "These three scenes record `metadata.navmesh: recomputed`; the other 15 kept their shipped navmesh." | `# Manifest metadata, not parquet. # from collections import Counter # Counter(json.load(open(p))["metadata"]["navmesh"] #         for p in glob("data/replica_renders/*/manifest.json")) # exp` |
| 5.5 | 26 | "Batch totals: 18 of 18 manifests validate" | `df = pd.read_parquet("outputs/eval/experiment_zero.parquet") print(df["scene"].nunique())  # expect 18 # Manifest validation itself is not recorded in the parquet; would need # len(glob("dat` |
| 5.6 | 26 | "5136 frames = 107 viewpoints x 48 frames" | `df = pd.read_parquet("outputs/eval/experiment_zero.parquet") frames = pd.unique(pd.concat([df["context_frame_id"], df["target_frame_id"]])) print(len(frames))                      # <= 5136;` |
| 5.7 | 27 | "One scene accepted 5 of 6 viewpoints under the depth quality filter, which the design permits." | `df = pd.read_parquet("outputs/eval/experiment_zero.parquet") vp = df.groupby("scene")["viewpoint"].nunique() print(vp[vp < 6])  # expect exactly one scene (frl_apartment_2) at 5 # Parquet ca` |
| 5.8 | 44 | "Background points the context camera really sees were therefore labelled disoccluded in a band about one pixel wide along every occlusion edge" | `# Width of a mislabelled band is a per-pixel mask property; the parquet stores # only covisible_fraction per pair. Nearest-vs-bilinear referee comparison would be: # mask_bilinear ^ mask_nea` |
| 5.9 | 47 | "At the smallest parallax bin the true disocclusion strip is only about ten pixels wide" | `df = pd.read_parquet("outputs/eval/experiment_zero.parquet") lo = df[df["parallax_bin"] == df["parallax_bin"].min()] print(lo["covisible_fraction"].describe()) # covisible_fraction is a scal` |
| 5.10 | 55 | "A center on a depth edge was lifted to a point in mid air, and its warp missed both surfaces by about half a patch at the rendered resolution." | `# Per-correspondence reprojection error in patch units is not a parquet column. # Would need per-sample records: \|uv_warped - uv_true\| / 14.0, median ~0.5 # The parquet grain here is pair-` |
| 5.11 | 58 | "The neighbour null could hand torch.multinomial an all-zero row on images under three patches wide." | `# Grid width is fixed at 37 patches for every cached frame, so this condition # never fires in the shipped run and leaves no parquet trace. # df[df.variant == "Neighbor-Patch"]["n"].min()  #` |
| 5.12 | 67 | "the derived frames move up to 0.4 times the median depth away with physics disabled" | `df = pd.read_parquet("outputs/eval/experiment_zero.parquet") r = df["baseline_m"] / df["context_median_depth_m"] print(r.max())  # expect ~0.4 for the translation program print(df.groupby("r` |
| 5.13 | 68 | "room_0 vp02 orbit_000 spans 0.04 to 6.46 m" | `# Per-frame depth min/max is not a parquet column (only context_median_depth_m). # Would need the per-frame depth sidecar: # s = json.load(open("data/replica_renders/room_0/depth_stats.json"` |
| 5.14 | 69 | "every room_0 vp05 frame sits under 1.7 m" | `df = pd.read_parquet("outputs/eval/experiment_zero.parquet") q = df[(df.scene=="room_0") & (df.viewpoint=="vp05")] print(q["context_median_depth_m"].max())  # expect < 1.7 # Parquet covers o` |
| 5.15 | 75 | "Applying the base view's own standard to every frame rejected 1208 of 5136 frames, 23.5 percent." | `# Frame-gate outcomes are not in the evaluation parquet at all. # Would need the per-frame measurement sidecar plus the rejected policy: # m = pd.read_parquet("outputs/frame_gate/measurement` |
| 5.16 | 78 | "rotation     1124/1391 pass, 19.2 percent rejected" | `# m = pd.read_parquet("outputs/frame_gate/measurements.parquet") # g = m[m.regime == "rotation"] # old_policy(g).sum(), len(g), 1 - old_policy(g).mean() # expect 1124, 1391, 0.192 # Sidecar ` |
| 5.17 | 79 | "translation  1479/1819 pass, 18.7 percent rejected" | `# m = pd.read_parquet("outputs/frame_gate/measurements.parquet") # g = m[m.regime == "translation"] # old_policy(g).sum(), len(g), 1 - old_policy(g).mean() # expect 1479, 1819, 0.187 # Sidec` |
| 5.18 | 80 | "orbit        1325/1926 pass, 31.2 percent rejected" | `# m = pd.read_parquet("outputs/frame_gate/measurements.parquet") # g = m[m.regime == "orbit"] # old_policy(g).sum(), len(g), 1 - old_policy(g).mean() # expect 1325, 1926, 0.312 # Sidecar abs` |
| 5.19 | 89 | "A pitch of plus or minus 15 degrees at 1.5 m eye height looks at floor or ceiling about a metre away, and the median depth band of 1.5 to 8 m throws  | `df = pd.read_parquet("outputs/eval/experiment_zero.parquet") rot = df[df.regime == "rotation"] print(rot["context_median_depth_m"].agg(["min","median","max"])) # The 15 deg pitch amplitude a` |
| 5.20 | 113 | "Under the corrected gate 5078 of 5136 frames are usable, 98.9 percent" | `# m = pd.read_parquet("outputs/frame_gate/measurements.parquet") # ok = usable_frame_ids(m) # len(ok), len(m), len(ok)/len(m)   # expect 5078, 5136, 0.989 # Sidecar absent. The evaluation pa` |
| 5.21 | 115 | "rotation     1381/1391 usable,  10 rejected" | `# m = pd.read_parquet("outputs/frame_gate/measurements.parquet") # g = m[m.regime == "rotation"]; ok = usable_frame_ids(g) # len(ok), len(g), len(g) - len(ok)   # expect 1381, 1391, 10 # Sid` |
| 5.22 | 116 | "translation  1790/1819 usable,  29 rejected" | `# m = pd.read_parquet("outputs/frame_gate/measurements.parquet") # g = m[m.regime == "translation"]; ok = usable_frame_ids(g) # len(ok), len(g), len(g) - len(ok)   # expect 1790, 1819, 29 # ` |
| 5.23 | 117 | "orbit        1907/1926 usable,  19 rejected" | `# m = pd.read_parquet("outputs/frame_gate/measurements.parquet") # g = m[m.regime == "orbit"]; ok = usable_frame_ids(g) # len(ok), len(g), len(g) - len(ok)   # expect 1907, 1926, 19 # Sideca` |
| 5.24 | 121 | "Translation moves it furthest, up to 0.4 times the median depth, and loses the most." | `df = pd.read_parquet("outputs/eval/experiment_zero.parquet") print(df.groupby("regime").apply(     lambda g: (g.baseline_m / g.context_median_depth_m).max())) # expect translation ~0.4, rota` |
| 5.25 | 126 | "Per-frame median depth has a p50 of 1.9 m across the 18 scenes, ranging from 1.5 to 3.0 m by scene, with per-scene p05 near 1.0 m and p95 between 1.9 | `df = pd.read_parquet("outputs/eval/experiment_zero.parquet") f = df.drop_duplicates("context_frame_id")[["scene","context_median_depth_m"]] print(f["context_median_depth_m"].quantile(0.50)) ` |
| 5.26 | 129 | "the translation program's 0.4 target means baselines of roughly 0.7 m" | `df = pd.read_parquet("outputs/eval/experiment_zero.parquet") t = df[df.regime == "translation"] print(t.loc[(t.baseline_m/t.context_median_depth_m).round(2) == 0.40, "baseline_m"].median()) ` |
| 5.27 | 136 | "transport allocates a full feature vector per splatted pixel, which is 824 MB per buffer at 518 px with 768 channels." | `# Pure memory-arithmetic claim, no parquet bearing. # 518*518*768*4 bytes = 824,180,736 = 824 MB (float32). Checkable by arithmetic # alone, not by any query. No column in the schema records` |
| 5.28 | 142 | "The weight matrix is 7.5 MB at the 37 by 37 grids in use." | `# Memory arithmetic: (37*37)^2 * 4 bytes = 7,496,644 = 7.5 MB (float32). # No parquet column records the transport implementation's buffer sizes.` |
| 5.29 | 143 | "Runtime is now nearly independent of feature width: 68 ms at 768 channels and 78 ms at 2048" | `# Timing claim; the schema has no duration column. # Would need a benchmark log: outputs/bench/transport_timing.parquet with # columns (channels, ms) and rows (768, 68), (2048, 78). # No out` |
| 5.30 | 144 | "where the old form would have needed about 6.6 GB per call at VGGT's width" | `# Memory arithmetic on a code path that no longer exists: # 518*518*2048*4 bytes = 2.20 GB per buffer; 6.6 GB implies three such buffers. # Unverifiable by query and, as stated, ambiguous ab` |
| 5.31 | 147 | "The per-pair pixel pipeline runs in float64 because manifest intrinsics and poses load as float64. Measured cost is 40 percent on CPU and far more on | `# Timing claim; no duration column in the schema. # Would need paired benchmark rows (dtype=float64 vs float32, seconds_per_pair) # in an outputs/bench/ table. Absent.` |
| 5.32 | 155 | "transporting them with ground-truth geometry beats copying them unwarped by 0.152 cosine on centered features" | `df = pd.read_parquet("outputs/eval/experiment_zero.parquet") d = df[(df.encoder=="dinov2_vitb14") & (df.path=="per_point")] w = lambda g: (g.cosine_centered_mean*g.n).sum()/g.n.sum() s = d.g` |
| 5.33 | 156 | "landing one patch off the correct location costs 0.072" | `df = pd.read_parquet("outputs/eval/experiment_zero.parquet") d = df[(df.encoder=="dinov2_vitb14") & (df.path=="per_point")] w = lambda g: (g.cosine_centered_mean*g.n).sum()/g.n.sum() s = d.g` |
| 5.34 | 159 | "their overall margin is -0.0001" | `df = pd.read_parquet("outputs/eval/experiment_zero.parquet") v = df[(df.encoder=="vggt_1b") & (df.path=="per_point")] w = lambda g: (g.cosine_centered_mean*g.n).sum()/g.n.sum() s = v.groupby` |
| 5.35 | 159 | "resolved by rotation angle it falls from +0.055 between 5 and 10 degrees to -0.279 beyond 40" | `df = pd.read_parquet("outputs/eval/experiment_zero.parquet") v = df[(df.encoder=="vggt_1b") & (df.path=="per_point")] w = lambda g: (g.cosine_centered_mean*g.n).sum()/g.n.sum() s = v.groupby` |
| 5.36 | 168 | "dinov2_vitb14  0.0852        0.2368   0.4769    0.5486  0.6285  +0.1516" (Random, Mean-Feature, No-Warp, Neighbor, Oracle, margin; pooled, centered,  | `df = pd.read_parquet("outputs/eval/experiment_zero.parquet") d = df[(df.encoder=="dinov2_vitb14") & (df.path=="per_point")] w = lambda g: (g.cosine_centered_mean*g.n).sum()/g.n.sum() s = d.g` |
| 5.38 | 168 | Centered Mean-Feature for DINOv2 reported as a finite value, "0.2368", in the pooled centered ladder table. | `df = pd.read_parquet("outputs/eval/experiment_zero.parquet") mf = df[(df.variant=="Mean-Feature") & (df.encoder=="dinov2_vitb14")] print(mf["cosine_centered_mean"].notna().sum())  # PROTOCOL` |
| 5.37 | 169 | "vggt_1b        0.1646        0.3193   0.7516    0.7316  0.7514  -0.0001" (Random, Mean-Feature, No-Warp, Neighbor, Oracle, margin; pooled, centered,  | `df = pd.read_parquet("outputs/eval/experiment_zero.parquet") v = df[(df.encoder=="vggt_1b") & (df.path=="per_point")] w = lambda g: (g.cosine_centered_mean*g.n).sum()/g.n.sum() s = v.groupby` |
| 5.39 | 169 | Centered Mean-Feature for VGGT reported as a finite value, "0.3193", in the pooled centered ladder table. | `df = pd.read_parquet("outputs/eval/experiment_zero.parquet") mf = df[(df.variant=="Mean-Feature") & (df.encoder=="vggt_1b")] print(mf["cosine_centered_mean"].notna().sum())  # PROTOCOL 3.7 r` |
| 5.40 | 172 | "The earlier parallax-only run gave +0.1395 and +0.0129" | `old = pd.read_parquet("outputs/eval/experiment_zero_run1.parquet") w = lambda g: (g.cosine_centered_mean*g.n).sum()/g.n.sum() for e in ["dinov2_vitb14", "vggt_1b"]:     s = old[(old.encoder=` |
| 5.41 | 172 | "on a sample half the size weighted differently" (7300 pairs versus 15084) | `old = pd.read_parquet("outputs/eval/experiment_zero_run1.parquet") new = pd.read_parquet("outputs/eval/experiment_zero.parquet") key = ["scene","context_frame_id","target_frame_id"] print(ol` |
| 5.42 | 173 | "per parallax bin the two runs agree to better than 0.015 everywhere" | `w = lambda g: (g.cosine_centered_mean*g.n).sum()/g.n.sum() def marg(df):     s = df[df.path=="per_point"].groupby(["encoder","parallax_bin","variant"]).apply(w).unstack()     return s["Oracl` |
| 5.43 | 178 | "Its floor sits at 0.787 while its correct answer sits at 0.800, and its one-patch-off null sits between them at 0.778, below the floor." | `df = pd.read_parquet("outputs/eval/experiment_zero.parquet") v = df[(df.encoder=="vggt_1b") & (df.path=="per_point")] w = lambda g: (g.cosine_centered_mean*g.n).sum()/g.n.sum() print(v.group` |
| 5.44 | 184 | "Warping it actively hurts in the two regimes with the largest image displacement, pure rotation at -0.046" | `df = pd.read_parquet("outputs/eval/experiment_zero.parquet") v = df[(df.encoder=="vggt_1b") & (df.path=="per_point") & (df.regime=="rotation")] w = lambda g: (g.cosine_centered_mean*g.n).sum` |
| 5.45 | 185 | "and the largest parallax bin at -0.013" | `df = pd.read_parquet("outputs/eval/experiment_zero.parquet") v = df[(df.encoder=="vggt_1b") & (df.path=="per_point") & (df.parallax_bin=="0.4+")] w = lambda g: (g.cosine_centered_mean*g.n).s` |
| 5.46 | 193 | "5-10      0.7711  0.4893  +0.2818           0.8804  0.8252  +0.0553" (DINOv2 ceiling/floor/margin, then VGGT ceiling/floor/margin) | `df = pd.read_parquet("outputs/eval/experiment_zero.parquet") w = lambda g: (g.cosine_centered_mean*g.n).sum()/g.n.sum() s = df[(df.path=="per_point") & (df.rotation_bin=="5-10")].groupby(["e` |
| 5.47 | 194 | "10-20     0.7025  0.3705  +0.3319           0.7570  0.7386  +0.0184" | `df = pd.read_parquet("outputs/eval/experiment_zero.parquet") w = lambda g: (g.cosine_centered_mean*g.n).sum()/g.n.sum() s = df[(df.path=="per_point") & (df.rotation_bin=="10-20")].groupby(["` |
| 5.48 | 195 | "20-40     0.5594  0.2798  +0.2796           0.5453  0.6373  -0.0920" | `df = pd.read_parquet("outputs/eval/experiment_zero.parquet") w = lambda g: (g.cosine_centered_mean*g.n).sum()/g.n.sum() s = df[(df.path=="per_point") & (df.rotation_bin=="20-40")].groupby(["` |
| 5.49 | 196 | "40+       0.4124  0.2228  +0.1896           0.2595  0.5384  -0.2788" | `df = pd.read_parquet("outputs/eval/experiment_zero.parquet") w = lambda g: (g.cosine_centered_mean*g.n).sum()/g.n.sum() s = df[(df.path=="per_point") & (df.rotation_bin=="40+")].groupby(["en` |
| 5.50 | 198 | "From 5-10 to 10-20 the floor falls faster than the ceiling, 0.119 against 0.068, so the margin grows." | `df = pd.read_parquet("outputs/eval/experiment_zero.parquet") d = df[(df.encoder=="dinov2_vitb14") & (df.path=="per_point")] w = lambda g: (g.cosine_centered_mean*g.n).sum()/g.n.sum() s = d.g` |
| 5.51 | 205 | "The parallax axis alone showed only the rising side of the hump and the first hint of the turn, +0.1476 at 0.2-0.4 falling to +0.1428 at 0.4+." | `old = pd.read_parquet("outputs/eval/experiment_zero_run1.parquet") d = old[(old.encoder=="dinov2_vitb14") & (old.path=="per_point")] w = lambda g: (g.cosine_centered_mean*g.n).sum()/g.n.sum(` |
| 5.52 | 211 | "Beyond 40 degrees, copying a feature from the same image position scores 0.538 while moving it to the same surface scores 0.260." | `df = pd.read_parquet("outputs/eval/experiment_zero.parquet") v = df[(df.encoder=="vggt_1b") & (df.path=="per_point") & (df.rotation_deg > 40)] w = lambda g: (g.cosine_centered_mean*g.n).sum(` |
| 5.53 | 213 | "Transport is 0.279 worse than doing nothing." | `df = pd.read_parquet("outputs/eval/experiment_zero.parquet") v = df[(df.encoder=="vggt_1b") & (df.path=="per_point") & (df.rotation_deg > 40)] w = lambda g: (g.cosine_centered_mean*g.n).sum(` |
| 5.54 | 231 | "Raw cosine reads VGGT's Oracle-Transport at 0.967." | `df = pd.read_parquet("outputs/eval/experiment_zero.parquet") v = df[(df.encoder=="vggt_1b") & (df.path=="per_point") & (df.variant=="Oracle-Transport")] print(round((v.cosine_mean*v.n).sum()` |
| 5.55 | 234 | "The floor is what turns it into a margin of 0.003" | `df = pd.read_parquet("outputs/eval/experiment_zero.parquet") v = df[(df.encoder=="vggt_1b") & (df.path=="per_point")] w = lambda g: (g.cosine_mean*g.n).sum()/g.n.sum() s = v.groupby("variant` |
| 5.56 | 250 | "That is what makes \"a random place in the other view of this room\" at 0.176 the right comparison against \"the same place\" at 0.787 and \"one patc | `df = pd.read_parquet("outputs/eval/experiment_zero.parquet") v = df[(df.encoder=="vggt_1b") & (df.path=="per_point")] w = lambda g: (g.cosine_centered_mean*g.n).sum()/g.n.sum() print(v.group` |
| 5.57 | 270 | "the ceiling itself decays with parallax, from 0.80 to 0.54 centered" | `df = pd.read_parquet("outputs/eval/experiment_zero.parquet") d = df[(df.encoder=="dinov2_vitb14") & (df.path=="per_point") & (df.variant=="Oracle-Transport")] w = lambda g: (g.cosine_centere` |
| 5.58 | 277 | "The splat-and-pool path matches the per-point path to within 0.003 everywhere" | `df = pd.read_parquet("outputs/eval/experiment_zero.parquet") w = lambda g: (g.cosine_centered_mean*g.n).sum()/g.n.sum() k = ["encoder","regime","parallax_bin","rotation_bin","variant"] s = d` |
| 5.59 | 282 | "The margin moves from 0.123 to 0.140" | `df = pd.read_parquet("outputs/eval/experiment_zero.parquet") d = df[(df.encoder=="dinov2_vitb14") & (df.path=="per_point")] for col in ["cosine_mean", "cosine_centered_mean"]:     s = d.grou` |
| 5.60 | 282 | "the growth with parallax from 0.065-to-0.134 to 0.075-to-0.147" | `df = pd.read_parquet("outputs/eval/experiment_zero.parquet") d = df[(df.encoder=="dinov2_vitb14") & (df.path=="per_point")] for col in ["cosine_mean", "cosine_centered_mean"]:     s = d.grou` |
| 5.61 | 283 | "and the zero bin from 0.248 to 0.288" | `df = pd.read_parquet("outputs/eval/experiment_zero.parquet") d = df[(df.encoder=="dinov2_vitb14") & (df.path=="per_point") & (df.parallax==0.0)] for col in ["cosine_mean", "cosine_centered_m` |
| 5.62 | 284 | "For VGGT centering quadrupled the margin, from 0.003 to 0.013, and left it an order of magnitude short." | `df = pd.read_parquet("outputs/eval/experiment_zero.parquet") v = df[(df.encoder=="vggt_1b") & (df.path=="per_point")] for col in ["cosine_mean", "cosine_centered_mean"]:     s = v.groupby("v` |
| 5.63 | 287 | "The two paths agree to 0.001 in the centered reading as well, 0.6642 per-point against 0.6647 splat-and-pool." | `df = pd.read_parquet("outputs/eval/experiment_zero.parquet") d = df[(df.encoder=="dinov2_vitb14") & (df.variant=="Oracle-Transport")] s = d.groupby("path").apply(lambda g: (g.cosine_centered` |
| 5.64 | 292 | "the zero bin pooled 7.5 to 60 degrees of yaw and its margin of 0.288 was an average across that whole range" | `df = pd.read_parquet("outputs/eval/experiment_zero.parquet") z = df[(df.encoder=="dinov2_vitb14") & (df.path=="per_point") & (df.parallax==0.0)] print(z["rotation_deg"].agg(["min","max"]).ro` |
| 5.65 | 305 | "rotation goes from 1 stratum and 40 pairs to 4 strata and 160" | `new = pd.read_parquet("outputs/eval/experiment_zero.parquet") k = ["scene","context_frame_id","target_frame_id"] r = new[new.regime=="rotation"].drop_duplicates(k) print(r.groupby("scene")[[` |
| 5.66 | 306 | "orbit from 4 strata and 160 to 13 strata and 478" | `new = pd.read_parquet("outputs/eval/experiment_zero.parquet") k = ["scene","context_frame_id","target_frame_id"] o = new[new.regime=="orbit"].drop_duplicates(k) print(o.groupby("scene").appl` |
| 5.67 | 307 | "translation is unchanged at 5 strata and 200" | `new = pd.read_parquet("outputs/eval/experiment_zero.parquet") k = ["scene","context_frame_id","target_frame_id"] t = new[new.regime=="translation"].drop_duplicates(k) print(t.groupby("scene"` |
| 5.68 | 307 | "The run grows from 7200 pairs to 15084" | `k = ["scene","context_frame_id","target_frame_id"] old = pd.read_parquet("outputs/eval/experiment_zero_run1.parquet").drop_duplicates(k) new = pd.read_parquet("outputs/eval/experiment_zero.p` |
| 5.69 | 308 | "and from about 9 minutes to about 19" | `# Wall-clock claim; the schema has no timing column. # Would need run metadata: json.load(open("outputs/eval/run_metadata.json")) # ["wall_seconds"] for each of the two runs, expect ~540 and` |
| 5.70 | 314 | "The first run scored 7300 pairs across 18 scenes." | `old = pd.read_parquet("outputs/eval/experiment_zero_run1.parquet") k = ["scene","context_frame_id","target_frame_id"] print(old.drop_duplicates(k).shape[0], old["scene"].nunique())  # expect` |
| 5.71 | 319 | "Measured on the caches, VGGT puts 0.9095 of a feature's norm in a single direction shared by every patch, against DINOv2's 0.4226." | `# Cache diagnostic, not a parquet quantity. Would be computed from cache/: # X = np.concatenate([np.load(p)["features"].reshape(C, -1).T for p in cache_paths]) # mu = X.mean(0); frac = np.li` |
| 5.72 | 321 | "Two vectors that each spend 91 percent of their length on one axis are forced above cosine 0.83 whatever the remaining 9 percent says" | `# Arithmetic bound, not a measurement: 0.91**2 - (1 - 0.91**2) = 0.656, while # the stated floor 0.83 corresponds to 0.91**2 = 0.828 (the same-sign case). # No query proves it; the correct c` |
| 5.73 | 323 | "Random-Patch 0.857, Mean-Feature 0.921, No-Warp-Copy 0.964, Oracle-Transport 0.967" (VGGT, raw cosine) | `old = pd.read_parquet("outputs/eval/experiment_zero_run1.parquet") v = old[(old.encoder=="vggt_1b") & (old.path=="per_point")] w = lambda g: (g.cosine_mean*g.n).sum()/g.n.sum() print(v.group` |
| 5.74 | 324 | "The margin of 0.003 measures the width of a saturated scale." | `old = pd.read_parquet("outputs/eval/experiment_zero_run1.parquet") v = old[(old.encoder=="vggt_1b") & (old.path=="per_point")] w = lambda g: (g.cosine_mean*g.n).sum()/g.n.sum() s = v.groupby` |
| 5.75 | 327 | "After subtracting the dataset mean, two random patches of one VGGT frame agree at 0.226, against 0.088 for DINOv2 centered." | `# Cache diagnostic, not an Experiment Zero row. Would be: # f = np.load(cache_path)["features"].reshape(C, -1).T - mu[None, :] # i, j = rng.choice(len(f), size=(2, N)); mean(cos(f[i], f[j]))` |
| 5.76 | 339 | "Experiment Zero's Random-Patch null for DINOv2 scored 0.265 while the independent cache diagnostic put two random patches of a frame at 0.2518" | `old = pd.read_parquet("outputs/eval/experiment_zero_run1.parquet") d = old[(old.encoder=="dinov2_vitb14") & (old.path=="per_point") & (old.variant=="Random-Patch")] print(round((d.cosine_cen` |
| 5.77 | 343 | "the per-point and splat-and-pool paths agreed to within 0.001 in every cell" | `old = pd.read_parquet("outputs/eval/experiment_zero_run1.parquet") w = lambda g: (g.cosine_mean*g.n).sum()/g.n.sum() k = ["encoder","regime","parallax_bin","variant"] s = old.groupby(k + ["p` |
| 5.78 | 348 | "the margin runs from 0.065 at the smallest parallax to 0.134 at the largest" | `old = pd.read_parquet("outputs/eval/experiment_zero_run1.parquet") d = old[(old.encoder=="dinov2_vitb14") & (old.path=="per_point")] w = lambda g: (g.cosine_mean*g.n).sum()/g.n.sum() s = d.g` |
| 5.79 | 351 | "the zero-parallax bin holds DINOv2's lowest absolute cosine, 0.699, with its largest margin, 0.248" | `old = pd.read_parquet("outputs/eval/experiment_zero_run1.parquet") d = old[(old.encoder=="dinov2_vitb14") & (old.path=="per_point")] w = lambda g: (g.cosine_mean*g.n).sum()/g.n.sum() s = d.g` |
| 5.80 | 353 | "that bin pools 7.5 to 60 degrees of yaw, which sweeps a surface point across two thirds of a 90 degree field of view" | `old = pd.read_parquet("outputs/eval/experiment_zero_run1.parquet") z = old[old.parallax == 0.0] print(z["rotation_deg"].agg(["min","max"]).round(1))  # expect 7.5, 60.0 # The "two thirds of ` |
| 5.81 | 363 | "A bright stripe drawn across the image rows peaks at patch row 17 of 37 with a flat column profile, and the same stripe drawn down the columns peaks  | `# Encoder grid-orientation probe, not an Experiment Zero row. # Proof is the gated test test_dinov2_grid_orientation_and_shape in tests/, # which asserts argmax over the 37x37 grid equals 17` |
| 5.82 | 369 | "Throughput at 518 px, batch 8, on an RTX 2080 Super: the model alone runs at 21 frames per second, the whole cache path at 8.9." | `# Throughput claim; the schema has no rate or duration column. # Would need outputs/cache/*/timing.json with {"model_fps": 21.0, # "end_to_end_fps": 8.9, "device": "RTX 2080 Super", "batch":` |
| 5.83 | 371 | "Moving the uint8 to float conversion and the normalization onto the GPU took the end to end rate from 7.0 to 8.9." | `# Before/after throughput claim; no parquet bearing. # Would need two timing.json records on identical hardware and input set, # with end_to_end_fps 7.0 and 8.9. Absent.` |
| 5.84 | 373 | "The full 5136 frame set is therefore about 10 minutes per encoder, at 2.1 MB per frame, so roughly 11 GB per encoder." | `# Extrapolation plus storage arithmetic; no parquet bearing. # Size check would be: sum(os.path.getsize(p) for p in glob("cache/dinov2_vitb14/**/*.npz")) # expect ~11e9 bytes over 5136 files` |
| 5.85 | 375 | "The largest token norm was within 1.1 times the median for both dinov2_vitb14 and dinov2_vitb14_reg." | `# Cache/probe diagnostic, not a parquet quantity. # f = np.load(probe_cache)["features"]; nrm = np.linalg.norm(f, axis=0) # nrm.max() / np.median(nrm)  # expect <= 1.1 for both encoders # Th` |
| 5.86 | 380 | "room_0 cached 288 frames, 6 viewpoints of 48, as fp16 [768, 37, 37], and validated against its manifest frame by frame." | `# Cache inventory claim. Would be: # len(glob("cache/dinov2_vitb14/room_0/*.npz"))  # expect 288 = 6 * 48 # np.load(p)["features"].shape, .dtype  # expect (768, 37, 37), float16 # cache/ abs` |
| 5.87 | 384 | "All 18 scenes cached and validated: 5136 frames, every one fp16 [768, 37, 37], frame counts matching the manifests, including frl_apartment_2 at 240  | `# Cache inventory claim. # sum(len(glob(f"cache/dinov2_vitb14/{s}/*.npz")) for s in scenes)  # expect 5136 # len(glob("cache/dinov2_vitb14/frl_apartment_2/*.npz"))            # expect 240 = ` |
| 5.88 | 387 | "Total wall time was about 3.5 minutes on one L40." | `# Wall-clock claim; no schema column. # NB internally inconsistent with line 388-390: 5136 frames at the stated # aggregate 25.9 fps is 198 s = 3.3 min, so the two are roughly consistent, # ` |
| 5.89 | 388 | "Per-scene rates ranged from 18.6 to 57.9 frames per second, a threefold spread with no relation to scene content or frame count, while the aggregate  | `# Per-scene throughput; no schema column. # Would need outputs/cache/dinov2_vitb14/per_scene_timing.parquet with # columns (scene, frames, seconds) -> fps = frames/seconds # fps.min(), fps.m` |
| 5.90 | 395 | "Its aggregator returns 2048 channels on the same 37 by 37 grid" | `# Cache shape claim. # np.load(glob("cache/vggt_1b/*/*.npz")[0])["features"].shape  # expect (2048, 37, 37) # cache/ absent. The parquet's encoder column distinguishes vggt_1b from # dinov2_` |
| 5.91 | 396 | "the estimated depth exported for Phase 4 is [518, 518] per frame with a confidence map beside it" | `# Depth export shape claim, and it bears directly on PROTOCOL 4.1. # np.load(glob("cache/vggt_1b/*/depth/*.npz")[0])["depth"].shape  # expect (518, 518) # If VGGT's native depth is already 5` |
| 5.92 | 397 | "All 5136 frames validate. The run took 13.6 minutes at 6.30 frames per second." | `# Count plus throughput. 5136 / 6.30 = 815 s = 13.6 min, internally consistent. # Would need len(glob("cache/vggt_1b/*/*.npz")) == 5136 and a timing record. # cache/ and outputs/ absent.` |
| 5.93 | 399 | "VGGT held 6.32 to 6.45 frames per second across every scene, a spread of two percent, where DINOv2 varied threefold over the same PNGs on the same fi | `# Per-scene throughput; no schema column. # Would need per_scene_timing for vggt_1b: fps.min()==6.32, fps.max()==6.45, # (6.45-6.32)/6.32 = 0.021 ("two percent"). # NB: the stated range 6.32` |
| 5.94 | 403 | "Only apartment_0 broke VGGT's uniformity, at 5.41, because it also paid for loading the 5 GB of weights." | `# Per-scene throughput plus a weights-size figure; neither is in the schema. # Would need per_scene_timing.loc["apartment_0", "fps"] == 5.41 and the on-disk # size of the VGGT checkpoint (~5` |
| 5.95 | 406 | "VGGT's wrapper originally ran the aggregator twice per batch ... and measured 2.02 frames per second. Taking the tokens with a forward hook instead r | `# Before/after throughput; no schema column. 6.30 / 2.02 = 3.12, internally consistent. # Would need two timing records for the same scene set under the two wrapper # implementations, recove` |